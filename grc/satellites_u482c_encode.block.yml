# auto-generated by grc.converter

id: satellites_u482c_encode
label: NanoCom U482C FEC encode
category: '[Satellites]/GOMspace'

parameters:
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: viterbi
    label: Viterbi
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: ['No', 'Yes']
-   id: scrambler
    label: Scrambler
    dtype: int
    default: '1'
    options: ['0', '1']
    option_labels: ['No', 'Yes']
-   id: rs
    label: Reed-Solomon
    dtype: int
    default: '1'
    options: ['0', '1']
    option_labels: ['No', 'Yes']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import satellites
    make: satellites.u482c_encode(${verbose}, ${viterbi}, ${scrambler}, ${rs})

file_format: 1
